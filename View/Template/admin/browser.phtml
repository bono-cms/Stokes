<?php

use Krystal\Form\Element;

?>

<div class="btn-toolbar">
    <div class="btn-group">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
        <span class="glyphicon glyphicon-list"></span> <?php $this->show('Menu'); ?> <span class="caret"></span>
      </button>
      <ul class="dropdown-menu">
         <li><a href="<?php echo $this->url('Stokes:Admin:Add@indexAction'); ?>"><i class="glyphicon glyphicon-plus"></i> <?php $this->show('Add stoke'); ?></a> </li>
         <li class="divider"></li>
         <li><a href="#" data-button="save-changes"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
         <li><a href="#" data-button="remove-selected"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
      </ul>
    </div>
</div>

<?php $this->loadBlock('items-per-page', array('caption' => 'Pages')); ?>

<form>
    <table class="table table-hover table-bordered table-striped table-condensed">
        <thead>
            <tr>
                <th class="text-center">
                    <?php if (!empty($stokes)): ?>
                    <input type="checkbox" />
                    <?php endif; ?>
                </th>
                <th class="text-center"><?php $this->show('#'); ?></th>
                <th class="text-center"><?php $this->show('Title'); ?></th>
                <th class="text-center"><?php $this->show('Default'); ?></th>
                <th class="text-center"><?php $this->show('SEO Enabled'); ?></th>
                <th class="text-center"><?php $this->show('Actions'); ?></th>
            </tr>
        </thead>
        
        <?php if (!empty($stokes)): ?>
        
        <tbody>
            <?php foreach ($stokes as $stoke): ?>
            <tr>
                <td class="text-center">
                    <input type="checkbox" name="toDelete[<?php echo $stoke->id; ?>]" />
                </td>
                <td class="text-center"><?php echo $stoke->id; ?></td>
                <td class="text-center"><?php echo $stoke->title; ?></td>
                <td class="text-center">
                    <?php echo Element::radio('default', $stoke->id, $stoke->default); ?>
                </td>
                <td class="text-center">
                    <?php echo Element::checkbox(sprintf('seo[%s]', $stoke->id), $stoke->seoEnabled); ?>
                </td>
                <td class="text-center">
                    <a data-button="view" href="<?php echo $stoke->slug; ?>"></a>
                    <a data-button="edit" href="<?php echo $this->url('Stokes:Edit@indexAction', $stoke->id); ?>"></a>
                    <a data-button="delete" href="#" data-category="main" data-id="<?php echo $stoke->id; ?>"></a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
        
        <?php endif; ?>

    </table>

    <?php if (empty($stokes)): ?>
    <div class="center-block">
        <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No stokes'); ?></h2>
    </div>
    <?php endif; ?>
    
</form>

<?php $this->loadBlock('paginator'); ?>

